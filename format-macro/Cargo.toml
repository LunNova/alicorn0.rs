[package]
name = "format-macro"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lib]
proc-macro = true

[dependencies]
quote = "1.0"
proc-macro2 = "1.0"
alicorn-format = { path = "../format" }

[dev-dependencies]
trybuild = "1.0"
imbl = "*"
