# Test lambda_implicit

# lambda_implicit creates a lambda with implicit type parameter
# Usage: lambda_implicit (T : Type) body
let identity = lambda_implicit (T : type)
	lambda_single (x : T)
		x

# Apply it to a number
(identity Number) 99
